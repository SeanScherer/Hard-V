# License: CC-BY-SA-4.0

# Note: the ^template thing works a bit funky / ends up being processed a little differently to how other "hot-editing" of existing units works (it feels like the templates are loaded by units in one pass, and only after that they get adapted -> ie effectively no change :P.
# Try working round this by making my own templates that load other templates :P ? (and then using those templates for my units). See if that works...
#Old note:
# z_messing-with-defaults-I-think-is-useless_as-not-loaded-all-^template-style-things-have-already-been-loaded-by-units-perhaps-ah-maybe-if-only-one-defaults+templates-file-existed-ie-don't-load-the-base-games...

^Pod:
	-RepairableNear:
	RepairableNear:
		RepairActors: fieldrepair
		CloseEnough: 2c0

# An alternative (or perhaps - complementary) way to do it would be so:

# ^LTMV:
#	Inherits: ^Pod
#	-RepairableNear:
#	RepairableNear:
#		RepairActors: fieldrepair
#		CloseEnough: 2c0


## In which case, don't forget to also change:
#Would need in /rules/pods.yaml -

#RIFLEMAN-TRACKED:
#	Inherits: ^LTMV
#..
#.
#.
#..
